# Dependencias da funcao serverless (Vercel, Python 3.14)
# Versoes minimas com wheels para Python 3.14 — nada de compilar Rust no build
fastapi>=0.118
pydantic>=2.12
sqlalchemy>=2.0.43
pg8000>=1.31.2
python-multipart>=0.0.20
httpx>=0.27
python-jose[cryptography]>=3.3.0
cryptography>=42.0          # mTLS Efi: abre o .p12 (PKCS#12) em runtime (Vercel usa este arquivo)
bcrypt>=4.1
