# Dependencias da funcao serverless (Vercel, Python 3.14)
# Versoes minimas com wheels para Python 3.14 — nada de compilar Rust no build
fastapi>=0.118
pydantic>=2.12
sqlalchemy>=2.0.43
pg8000>=1.31.2
python-multipart>=0.0.20
httpx>=0.27
python-jose[cryptography]>=3.3.0
bcrypt>=4.1

# Apenas para rodar localmente
uvicorn[standard]>=0.30
python-dotenv>=1.0
pytest>=8
